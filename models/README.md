# Model files

Place the pretrained artifacts here before inference:

- `vit_seq2seq_.pt` - ViT + Transformer checkpoint (~297 MB)
- `lora_model_qwen3vl.zip` - Qwen3-VL LoRA adapter (~165 MB)

They are excluded from Git because GitHub rejects individual files larger than 100 MB.
